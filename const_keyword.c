#include <stdio.h>

int main(){
  const int PI = 3.14; // good practice to give constants capital names
  const int SPEED;
  SPEED = 300; // you can't do this ig, something like this from a w3 example

  // when you declare a constant variable, it must be assigned with a value, it cannot be left empty
  const int minutes; // you can't do this
  return 0;
}